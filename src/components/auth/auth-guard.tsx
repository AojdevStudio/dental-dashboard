// Placeholder for AuthGuard component
import type React from "react";

export function AuthGuard({ children }: { children: React.ReactNode }) {
  // Add authentication logic here
  // For now, just renders children
  return <>{children}</>;
}
